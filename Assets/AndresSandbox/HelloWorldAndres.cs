using UnityEngine;

public class HelloWorldAndres: MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello World!");
		Debug.Log("Sayonara World!");
	}

}
